# INTER-LAN P3 — Code Writer Continuation

ROLE CALL: **CODE WRITER**

Technical Authority: **Kirch Ivan Balite**

Repository:

`Kirch-Nairu/INTER-LAN-CROSS.OS-COMMUNICATION-TOOL`

Active phase:

**P3 — Group Chat**

Target branch:

`KIRCH-INTERLAN-P3-GROUP-CHAT`

Accepted main observed at governance reconciliation:

`d821536cec77b0b56f16bc6a02b4ffe935dfa8a0`

Pre-governance source anchor:

`939fa64e1613ded90366c01d18928493024f7b0b`

The exact remote branch HEAD is movable and MUST be re-read before mutation.

## KIRION authority

Pinned doctrine:

`Kirch-Nairu/KIRION-FORGE@44eb57e5b45b343be0033bf22a7a5e74d543c01a`

Read before substantial work:
1. `AGENTS.md`
2. `.forge/AUTHORITY.md`
3. `.forge/SSOT_CURRENT.md`
4. `.forge/NEST.md`
5. `00-HANDOFF/INTERLAN_V1_CODE_CAMPAIGN.md`
6. `00-HANDOFF/INTERLAN_INVARIANTS.md`
7. `00-HANDOFF/INTERLAN_KNOWN_RISKS.md`

## Owned scope

P3 group-chat production source, contracts, migrations, repository-local executable checks, and P3 durable project memory.

## Do not seize

- P4 file-hosting implementation;
- P5 Telegram archive implementation;
- P6 UI/product composition beyond interfaces required to keep P3 client contracts coherent;
- new CI/CD workflow architecture;
- deployment/release automation;
- P3 acceptance/promotion authority.

## Required behavior

Continue until P3 exit conditions are technically satisfied:
- creator OWNER;
- non-member read/send denial;
- role mutation boundaries;
- immediate removal HTTP/realtime revocation;
- persist-before-broadcast;
- idempotent message IDs;
- durable ordered history/catch-up;
- current-membership realtime targeting;
- concurrency and IDOR executable proof.

## Current source state

Repository inspection shows substantial P3 implementation already exists, including group authority, messages, receipts, edit/delete, realtime, typed clients, recent history/backfill, and P3 check/smoke/concurrency projects.

Do not rebuild those from conversation memory. Inspect current files and neighboring contracts before each next slice.

## Priority continuation

1. audit P3 implementation against exit conditions and invariants;
2. harden any concurrency/authorization race capable of violating current authority;
3. ensure restart/history and IDOR checks cover the hardened behavior;
4. keep aggregate verification topology coherent;
5. update durable memory after material state changes.

## Evidence rule

Do not claim PASS unless observed. If validation cannot be run, record `NOT RUN` / `NOT VERIFIED` and continue source work where safe.

## Stop boundaries

Stop feature expansion for:
- security/authorization defect;
- persistence/data-integrity defect;
- migration defect;
- exact-authority drift that invalidates the active branch assignment.

Fix the defect in the current P3 scope before continuing.

## Promotion

Do not self-accept or self-promote P3. Return candidate identity and evidence when P3 is technically ready for acceptance.
